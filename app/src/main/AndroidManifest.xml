<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.phirered2015.homedoctor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.TestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity"/>
        <activity android:name=".activity.SignUpActivity" android:label="@string/sign_up_kr"/>
        <activity android:name=".activity.TutorialActivity"/>
        <activity android:name=".activity.LoginActivity"/>
        <activity android:name=".activity.DeliverStateActivity" android:label="@string/sign_up_kr"/>
        <activity android:name=".activity.SplashActivity"/>
        <activity android:name=".activity.DetailActivity"/>
        <activity android:name=".activity.BasketActivity" android:label="@string/basket_kr"/>
        <activity android:name=".activity.MyPageActivity" android:label="@string/my_page_kr"/>
        <activity android:name=".activity.PayInfoActivity" android:label="@string/pay_info_kr"/>
        <activity android:name=".activity.PaySuccessActivity" android:label="@string/pay_success_kr"/>
        <activity android:name=".activity.PayActivity"/>

    </application>

</manifest>